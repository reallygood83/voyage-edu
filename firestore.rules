rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 중 임시 규칙: 모든 읽기/쓰기 허용 (테스트 목적)
    // 프로덕션 환경에서는 반드시 더 제한적인 규칙을 사용해야 합니다
    match /{document=**} {
      allow read, write: if true;
    }
  }
}